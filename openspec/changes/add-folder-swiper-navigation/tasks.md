## 1. 內容重組與共用情境

- [x] 1.1 將 `App.tsx` 現有指標、比較、診斷、研究與政策區拆為五頁內容元件，沿用原快照與文字限制；驗證五頁的內容分工符合 design 第 1 節，沒有生成假 Risk 或政策。
- [x] 1.2 建立穩定頁面 ID、固定順序及預設指標頁；以元件測試驗證五個名稱、唯一目前頁、首次進入及未就緒頁仍可閱讀。
- [x] 1.3 提升職業選取至共用資訊列，連結風險頁既有職業列表；測試從論證頁換職業後不換頁，其他四頁及詳情皆使用同一職業與快照。
- [x] 1.4 確保換頁與職業選取不新增 refresh／生成副作用；保留既有離線快照及錯誤狀態測試，驗證切遍五頁不額外取資料、日期與三筆變化率不變。

## 2. 檔案夾布局與非循環導覽

- [x] 2.1 實作頂部五項膠囊導覽、中央展開夾與深藍綠外框／淺色閱讀區，移除舊直式主導航；在寬版截圖驗證單頁聚焦、文字未裁切及參考圖的檔案夾輪廓。
- [x] 2.2 實作依目前索引排列的左右收合夾，桌機每側最多兩個、近頁靠內；以五組排列測試核對 design 第 3 節表格，確認不存在的方向無假目標且按鈕可點區不重疊。
- [x] 2.3 統一頂部與側邊切換入口，支援任意直達、同頁無副作用及首尾夾限；測試指標直達報告、點側邊遠頁、重點目前頁、首尾越界均有正確結果。
- [x] 2.4 依 design 第 8 節實作新 hash、舊錨點轉換、無效值回退及瀏覽歷史同步；測試直開、重新整理、上一頁／下一頁、同頁不重複 push，外部導航不形成歷史迴圈。

## 3. 過渡動畫與閱讀位置

- [x] 3.1 加入約 420ms 的協同位移、外夾收合／展開及導覽底色過渡，內文不縮扁；在一般動態模式錄影核對前後方向、遠距一次直達、無中途空白及固定舞台尺寸。
- [x] 3.2 實作最後一次有效選取勝出的中斷／取消處理；用可控制完成事件的測試驗證「風險 → 報告 → 論證」快速改選、過期完成事件、尺寸改變及卸載後不回寫舊目標。
- [x] 3.3 依使用者新決議，網站預設開啟動畫，提供可保存的動畫效果開關；測試已保存關閉偏好、動畫中關閉與儲存受限仍可操作，均無等待動畫事件才解鎖的情況。
- [x] 3.4 以快照／職業／分頁保存閱讀位置與一般展開項，處理首次組合及高度夾限；測試論證離開再返回、跨職業／版本不混用、點目前頁不回頂，以及切換 scroll root 不丟失目前分頁。

## 4. 鍵盤、觸控與詳情

- [x] 4.1 建立 tablist／tab／tabpanel、手動啟用鍵盤模式、有效焦點樣式與「跳至目前頁內容」入口；測試左右鍵／Home／End 只移焦點，Enter／Space 啟用且 Tab 不進入隱藏內容。
- [x] 4.2 隔離退場與非目前頁的事件及輔助閱讀順序，處理側夾消失後焦點；以鍵盤與可及樹確認只有目前頁內容可操作，側夾啟用後焦點回同名頂部分頁。
- [x] 4.3 在標題非互動區加入受限觸控換頁，保留 wheel／縮放／內文原生操作；測試 48px 與水平比例門檻、未達門檻、取消、多指、首尾及互動子項，瀏覽器確認長內容滾到底與水平 wheel 都不換頁。
- [x] 4.4 整合既有單一 modal 的 Esc、關閉鈕、遮罩與焦點回復；測試 modal 開啟時背景導航／手勢／捲動被隔離，外部 hash 變更能關閉失效詳情並回到有效焦點。

## 5. 響應式與整合驗收

- [x] 5.1 實作寬版每側兩個、中版每側一個、手機隱藏側夾的布局；於 1440／1024／390／320px 檢查五頁皆可達、頁名完整、字級及 44px 點擊區，文件無水平溢出。
- [ ] 5.2 完成固定舞台的單一閱讀捲動與低高度文件捲動回退；於低高度視窗及 200% 縮放驗證內容可讀、導覽不遮焦點、沒有兩個競爭的主要垂直捲動區。
- [x] 5.3 遷移既有錨點專屬測試並保留資料／缺值／刷新／modal 回歸案例，補齊目前 capability 的情境覆蓋；在 `frontend` 執行 `npm run check`，記錄實際結果而非沿用舊 11 項通過紀錄。
- [x] 5.4 使用測試專用長內容及局部錯誤案例做完整瀏覽器流程驗收，記錄一般／reduce 模式與裝置尺寸；確認無空白、重疊可點內容、誤換頁、未清理動畫或控制台例外，測試資料不得進入 production 快照。

## 6. 文件交接與交付

- [x] 6.1 於本案核准並實作後，同步 `define-youth-policy-dashboard` 及前端需求文件中衝突的三區／錨點布局段落，標記被此 change 取代；以文件差異確認職業、模型、真實資料與來源限制未被刪除或改口徑。
- [x] 6.2 更新 `docs/FRONTEND_IMPLEMENTATION_STATUS.md` 與後端交接草稿的五頁消費對應，保留未完成的模型、來源、正式 API 與報告服務項目；驗證清楚區分 UI 完成、資料待補及服務待接，不將規劃欄位寫成既有 API。
- [x] 6.3 交付實際畫面、測試紀錄及簡短人工驗收路徑（頂部直達、側夾、首尾、滾輪、閱讀恢復、換職業、手機）；確認未混入後端／公式／部署變更，再請使用者驗收。commit、push、歸檔或部署另依使用者授權執行。

補驗：5.2 的低高度與 720×450 CSS px（1440×900 的 200% 等效）已通過；自動化瀏覽器未接受原生縮放快捷鍵，原生 200% 縮放仍待人工確認，故此項不勾選。詳見 docs/FOLDER_UI_VALIDATION.md。
